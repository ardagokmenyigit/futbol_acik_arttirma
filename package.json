{
  "name": "football-auction-league",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Gerçek zamanlı çok oyunculu futbolcu açık artırma ligi",
  "workspaces": [
    "packages/shared",
    "packages/server",
    "packages/client"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently -n server,client -c blue,magenta \"npm run dev -w @fal/server\" \"npm run dev -w @fal/client\"",
    "build": "npm run build -w @fal/shared && npm run build -w @fal/server && npm run build -w @fal/client",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "concurrently": "^9.1.0",
    "eslint": "^9.17.0",
    "eslint-config-prettier": "^9.1.0",
    "globals": "^15.14.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.1"
  }
}
