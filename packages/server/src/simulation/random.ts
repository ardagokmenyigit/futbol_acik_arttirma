/**
 * Deterministik PRNG — TEK KAYNAK `@fal/shared`'dadır.
 * Simülatörle aynı gerekçe (bkz. `simulator.ts`): iki ayrı kopya, sessizce
 * ayrışan iki ayrı oyun demekti.
 */
export { createPRNG, stringToSeed } from '@fal/shared';
